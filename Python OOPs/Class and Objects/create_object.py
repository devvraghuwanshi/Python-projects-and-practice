# Object : A bundle of related attributes(variables) and methods(functions).
# You need to create class to create many objects.

# Syntax: Use class to make object.

# obj1 = Myclass()

# delete object : del obj_name
# You can create multiple object from same class.

class MyClass:
    x = 5

obj1 = MyClass()
print(obj1.x)